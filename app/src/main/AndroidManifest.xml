<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.linka.donor">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Register" />
        <activity android:name=".RegisterForm" />
        <activity
            android:name=".Login"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Search" />
        <activity android:name=".MainPage" />
        <activity android:name=".History" />
        <activity android:name=".nD" />
        <activity android:name=".pD" />
        <activity android:name=".Transfer" />
        <activity android:name=".Withdraw" />
        <activity android:name=".Popup" />
        <activity android:name=".bank_register" />
        <activity android:name=".bank_MainPage" />
        <activity android:name=".bank_pending" />
        <activity android:name=".bank_startDonation" />
        <activity android:name=".bank_DispenseBlood" />
        <activity android:name=".bank_checkInventory"></activity>
    </application>

</manifest>